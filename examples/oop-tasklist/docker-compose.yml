services:
  app:
    build:
      context: ../..
      dockerfile: examples/oop-tasklist/Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
