name: dom-template CI

on:
  push:
    branches: [main]
    paths:
      - "packages/dom-template/**"
      - "package-lock.json"
      - "scripts/generate-coverage-badge.mjs"
      - ".github/workflows/dom-template-ci.yml"
  pull_request:
    paths:
      - "packages/dom-template/**"
      - "package-lock.json"
      - "scripts/generate-coverage-badge.mjs"
      - ".github/workflows/dom-template-ci.yml"
  workflow_dispatch:

concurrency:
  group: dom-template-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build and test dom-template
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build dom-template
        run: npm run build --workspace=packages/dom-template

      - name: Run tests with 100% coverage threshold
        run: npm run test:coverage --workspace=packages/dom-template

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dom-template-coverage-${{ github.run_id }}
          path: packages/dom-template/coverage/
          if-no-files-found: ignore
          retention-days: 14

      - name: Generate coverage badge data
        run: node scripts/generate-coverage-badge.mjs packages/dom-template

      - name: Commit coverage badge
        if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add packages/dom-template/coverage-badge.json
          git diff --staged --quiet || git commit -m "chore(dom-template): update coverage badge [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main
